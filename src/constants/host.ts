const host = process.env.EXPO_PUBLIC_BASE_URL;
const port = process.env.EXPO_PUBLIC_PORT_IMG;
const version = process.env.EXPO_PUBLIC_BASE_VERSION;
export const BASE_URL_PREVIEW_IMG = `${host}${port}${version}/cust/images/preview`;
